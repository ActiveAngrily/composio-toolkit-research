# scripts/fetch_composio_registry.py needs NOTHING - standard library only.
# These are for the research agent and scoring code.
httpx>=0.27
tenacity>=8.2
pydantic>=2.7
anthropic>=0.40
python-dotenv>=1.0
